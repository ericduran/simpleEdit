/**
 * I wanted to put the window application logic.
 * EX. We need specific logic for working with multiple
 * simple edit based window, (user might have one file open in one window
 * and another file open in a different window).
 *
 */
